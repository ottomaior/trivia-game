# Postgres connection string. Optional in local dev: without it the server runs
# with in-memory persistence stubs (nothing is saved).
DATABASE_URL=postgres://trivia:trivia@localhost:5432/trivia
PORT=3000
