# Postgres connection string. Optional for local play: without it the server
# uses the bundled starter questions in memory (nothing is saved).
DATABASE_URL=postgres://trivia:trivia@localhost:5432/trivia
PORT=3000

# Only for `pnpm gen` (question generation). Not needed by the game server.
ANTHROPIC_API_KEY=
